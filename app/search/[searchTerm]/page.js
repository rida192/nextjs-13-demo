import React from "react";

const SearchTerm = async ({ params: { searchTerm } }) => {
  // const searchReasults = await search(searchTerm);
  console.log(searchTerm);
  return <div>SearchTerm</div>;
};

export default SearchTerm;
